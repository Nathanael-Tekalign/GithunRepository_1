
// Define a new class in lib_rary.js
class Library_1{
    // Define global "properties" of class Library_1:
    lib_name;
    //books = [];

    // Constructor method that takes a `name` parameter
    constructor(lib_name) {
      // Set the name property of new instances to the name given
      this.lib_name = lib_name;
    }
  
    // Method that logs the value of the name property to the console
    logName() {
      console.log(this.lib_name);
    }
  }



/*
* a. the class should contain a constructor method that takes a name parameter.
* b. The constructor should set the name property of a new instance to the name given through the paramenters.
* c. The class should have a method that logs the value of the name property of the object to console.
*/



// class Book {
//     title;
//     author;
//     genre;
// }
  

















  // Create a new instance of the class with the name "Alice"
  //let alice = new Library_1("Alice");
  
  // Call the logName method on the instance
  //alice.logName(); 
  // Prints "Alice" to the console